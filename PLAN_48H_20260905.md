# Plan 48hs — 2026-09-05 → 2026-09-07

**Contexto**: Claude Code (yo) cerca del límite de créditos. OpenCode, Cline y
Kilo están en free tier — el trabajo de las próximas 48hs recae en ellos. Este
documento es la hoja de ruta que reemplaza mi supervisión turno-a-turno.

**Regla que no cambia para nadie**: ejecutor ≠ verificador. Ningún commit se
integra a `main` sin que otro agente (o Boris) verifique con datos reales —
nunca aceptar un self-report de "listo" sin correr el test/artefacto.

---

## Estado verificado ahora mismo (2026-09-05, antes de dispatch)

- `tmp-merge-check` tiene 3 merge commits limpios (Kilo A0+B1, Cline
  A1+A4+A6+A7+A8+A9, OpenCode A3+A5) más el fix del commit incompleto
  (`c0c8c95`). Pendiente: correr suite completa una vez más y mergear a
  `main` + push.
- Ninguna terminal de agente sigue viva sobre los worktrees de
  `fortress_core` (`orca worktree ps` las muestra `live:0`) — hay que
  reabrirlas.
- B4 ya está commiteado (`2d3c888`). B5 (mde_power.py + análisis del gate de
  diciembre) está completo pero SIN commitear en el worktree de
  Cline (`fundamentales-automatizado`).
- Boris ya decidió (2026-09-05): el gate de diciembre NO exige DSR>=0.90
  sobre los ~60 días de paper (matemáticamente casi imposible, ver
  `ANALISIS_MDE_GATE_DICIEMBRE_2026.md` de Cline). Se adopta la opción 1 de
  ese mismo análisis: DSR>=0.90 se mantiene SOLO sobre las ventanas OOS
  históricas (W1/W2/W3, ~500 días c/u, donde siempre se usó); diciembre pasa
  a verificar el TUBO (racha de días limpios + fill rate/slippage de A5 +
  coherencia paper-vs-señal), no a re-probar el edge desde cero. Se sigue
  trabajando en paralelo para acumular más/mejor data (intradía, IV) con
  aspiración de 0.9 a futuro — sin bloquear nada mientras tanto.

---

## Kilo — worktree `test-kilo-orca`

**Ticket A2 — Contador de días limpios automático** (`PLAN_REMEDIO_BRECHAS_20260903.md` línea 43+)
1. Leer la definición completa del ticket en ese archivo (sección A2).
2. Depende de A1 (reconciler) — ya mergeado en `tmp-merge-check`, va a
   `main` en paralelo (ver sección OpenCode-orquestador abajo). Si `main`
   todavía no tiene A1 cuando empiecen, trabajar sobre `tmp-merge-check`
   como base y avisar en `SESSION_LOG.md`.
3. Reemplaza el conteo manual del racha "Regla 0" por un script/función que
   lea `pipeline_health_*` + `reconcile.unexplained` y emita el contador
   automáticamente (mismo criterio de 3 condiciones ya documentado en
   ROADMAP.md).
4. Test real (no mockeado del todo): al menos un caso que alimente 3 días
   sintéticos completos y confirme que el contador avanza, y uno que rompa
   una condición y confirme que NO avanza.
5. Commitear con mensaje convencional. NO mergear a main ustedes mismos —
   eso lo hace quien orqueste (ver abajo).

---

## Cline — worktree `fundamentales-automatizado`

**Paso 1 — cerrar B5 (ya está hecho, falta commitear)**
1. `git add backend/scripts/mde_power.py backend/tests/test_mde_power.py backend/app/core/trial_registry.py scripts/audit_trial_budget.py ANALISIS_MDE_GATE_DICIEMBRE_2026.md` (los que correspondan según lo que ya esté en disco).
2. Correr `pytest tests/test_mde_power.py -q` y pegar la salida real en
   `SESSION_LOG.md` antes de commitear — no commitear sin haber corrido el
   test vos mismo.
3. Commit convencional (`feat(B5): gate de potencia ex-ante MDE + análisis
   gate diciembre`).

**Paso 2 — redactar el criterio nuevo del gate de diciembre (C1)**
Boris ya tomó la decisión (ver sección de estado arriba). Implementarla en
texto, en dos archivos:
1. `ROADMAP.md`: en la sección del gate/Regla 0 de diciembre, reemplazar
   "DSR>=0.90 en >=2/3 ventanas sobre ~60 días de paper" por:
   - DSR>=0.90 se evalúa y se mantiene válido SOLO sobre las ventanas OOS
     históricas pre-corte (W1/W2/W3) — es el criterio que siempre se usó
     ahí y sigue vigente sin cambios.
   - El gate de diciembre (paper prospectivo, ~60-90 días) verifica en
     cambio: (a) racha de días limpios ininterrumpida (condiciones a+b+c ya
     definidas), (b) fill rate/slippage medido (A5) dentro de lo esperado,
     (c) coherencia paper-vs-señal (las señales que debieron dispararse,
     se dispararon; sin discrepancias no explicadas).
   - Aclarar explícitamente: esto NO es una segunda prueba estadística de
     edge — es un chequeo operativo de que el tubo corrió limpio. La
     prueba de edge sigue siendo la de las ventanas históricas.
   - Agregar una línea sobre el trabajo paralelo: se sigue acumulando data
     (intradía B1, IV B2) con la aspiración declarada de eventualmente
     alcanzar DSR>=0.90 también sobre ventanas de paper más largas, sin que
     eso bloquee el gate de diciembre.
2. `PLAN_REMEDIO_BRECHAS_20260903.md`: actualizar la definición de C1 (buscar
   "1/12" o "C1" en el archivo) con el mismo texto, citando
   `ANALISIS_MDE_GATE_DICIEMBRE_2026.md §6 opción 1` como fundamento.
3. Citar en ambos archivos el mismo análisis (`ANALISIS_MDE_GATE_DICIEMBRE_2026.md`)
   como la fuente — no reinventar el argumento, solo aplicarlo.
4. Commit separado del de B5 (`docs(C1): re-especifica criterio gate
   diciembre — DSR sobre OOS histórico, paper verifica tubo no edge`).

**No hacer**: no tocar código de `backtest_engine.calculate_metrics` ni el
estimador de DSR — el análisis dejó claro que el estimador está bien, lo que
cambia es dónde se aplica el criterio.

---

## OpenCode — worktree `test-opencode-orca`

**Paso 1 — re-verificar el fix de B2 (regresión de suite, 2 bugs)**
1. Confirmar que estos dos fixes están aplicados y commiteados:
   - `IV_SYMBOLS` ya no es una lista hardcodeada desincronizada (debe
     derivarse de la misma fuente que el universo real de símbolos).
   - El test con fechas hardcodeadas ("bomba de tiempo") ahora mockea
     `datetime.now()` en vez de usar fechas fijas.
2. Correr la suite de B2 completa (no solo el test que falló) y pegar
   salida real en `SESSION_LOG.md`.
3. Si falta alguno de los dos, aplicarlo y volver a correr antes de
   commitear.

**Paso 2 — si Paso 1 ya estaba commiteado y verde**: tomar el siguiente
ticket libre de Fase B: **B6 — Contrato de señal única con equivalencia
dorada** (`PLAN_REMEDIO_BRECHAS_20260903.md` línea ~119). Leer la definición
completa antes de empezar. Recordatorio del plan: "B6 solo con golden
bit-idéntico + actualización de manifiesto declarada (reinicia el contador
— o se difiere al 1/12 si la equivalencia no es perfecta)" — si no se puede
lograr equivalencia bit-idéntica en el tiempo disponible, documentar el
estado y diferir en vez de forzar un commit que reinicie la racha de días
limpios sin necesidad.

---

## Hoja de ruta para OpenCode como orquestador (si Claude Code se queda sin créditos)

Si en algún momento de estas 48hs Claude Code deja de responder o Boris
indica que se acabaron los créditos, OpenCode toma el rol de coordinador con
estas reglas, en este orden:

1. **Nunca aceptar un self-report sin correr el test/artefacto vos mismo**
   (incluido el tuyo propio de hace un rato — volvé a correrlo antes de
   decir "listo").
2. **Confirmar la suite completa antes de mergear**: `cd backend &&
   ./.venv/bin/python -m pytest -q` sobre `tmp-merge-check` (o `main` una
   vez mergeado). Único resultado aceptable: solo las 4 fallas
   preexistentes conocidas (`test_backtest_2023_inalterado_por_ajuste_futuro`
   + 3 en `test_predict_cache.py` por `motor` faltante en un fixture
   `SimpleNamespace` desactualizado). Cualquier otra falla nueva: no
   mergear, reportar y esperar.
3. **Mergear `tmp-merge-check` a `main` y pushear** una vez confirmado el
   punto 2 — con el pie de commit de atribución que ya está fijado (ver
   `Claude-Session` en los commits recientes, mantener el mismo formato o
   el que Boris haya indicado directamente a vos).
4. **Ir marcando en `SESSION_LOG.md` y `ROADMAP.md`** cada ticket que se
   cierre, con la fecha real.
5. **Dispatch de tickets nuevos**: una vez agotada la lista de arriba
   (A2, B5, B6, C1), el siguiente en la cola es **A2→ya asignado**, y
   después evaluar si conviene B3 (feature store) o B7 (universo
   point-in-time, opcional) — leer sus definiciones en
   `PLAN_REMEDIO_BRECHAS_20260903.md` antes de asignar.
6. **No tomar decisiones de gobernanza por tu cuenta** (cambiar criterios
   de gates, bajar barras estadísticas, decisiones irreversibles) — esas
   son de Boris. Si aparece una, presentarla y esperar, igual que hizo
   Claude Code con el DSR de diciembre.
7. **Cuidar créditos de los demás agentes free**: si alguno pega contra su
   límite diario (como pasó con DeepSeek en Cline), no insistir — avisar a
   Boris y esperar a que rote el modelo, o rotar vos mismo si tenés esa
   capacidad confirmada.
8. **Riesgo de infraestructura pendiente, sin mitigar todavía**:
   `scripts/auto_backup.sh` corre `git reset --mixed HEAD` cada 600s vía
   el launchd job `com.fortresscore.autobackup` — puede pisar un merge en
   curso (ya pasó una vez, corregido con `commit --amend`). Si vas a hacer
   una operación git larga (merge, rebase), considerá pausar ese job
   (`launchctl unload`) mientras dura y reactivarlo después.

---

## Qué NO hacer ninguno de los tres (validez para las 48hs completas)

- No mergear nada a `main` sin que la suite completa haya corrido limpia
  (ver criterio arriba).
- No bajar ningún otro criterio estadístico (DSR, MDE, PBO) sin que Boris
  lo pida explícitamente — el de diciembre ya fue una decisión suya,
  explícita y documentada arriba; no es precedente para tocar otros.
- No conectar a broker real bajo ninguna circunstancia (sigue bloqueado).
- No fabricar o inflar un resultado de test para poder commitear.
