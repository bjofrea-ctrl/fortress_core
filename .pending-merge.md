# Pending Merge: Semana 2 — launchd plist para pipeline diario

## Archivos nuevos
- `scripts/daily_signal_pipeline.sh` — Shell script con lógica de fase por hora ET:
  - 09:35 ET: ENTER si es primer hábil del mes, health si no
  - 15:40 ET: EXIT si es último hábil del mes, health si no
  - 22:10 ET: DECIDE si es último hábil del mes, health si no
  - Fuera de ventanas: health (chequeo de rutina)
- `scripts/com.fortresscore.pipeline.plist` — launchd plist (mismo patrón que data_updater):
  - Corre 3 veces al día: 09:35, 15:40, 22:10 ET
  - Labels: `com.fortresscore.pipeline`
  - Logs: `scripts/pipeline_launchd.log`

## Cambio en script existente
- `scripts/daily_signal_pipeline.sh` — Reemplazado (antes corría `--phase auto` estático, ahora tiene routing por hora + check calendario)

## Fix de bug (ya commiteado a main)
- `backend/scripts/pipeline_daily_signal.py` — `_make_client_factory` ahora pasa `api_key`/`secret_key` de Settings al constructor de `AlpacaPaperClient` (mismo fix que `_open_client_and_budget`)

## Tests
- 19/19 tests pasan (`test_pipeline_daily_signal.py`)
- Shell script testeado manualmente: detecta fase correctamente por hora ET
- Calendar check funciona: identifica primer/último hábil del mes

## Antes de instalar (Boris revisa)
1. Revisar `scripts/daily_signal_pipeline.sh` — la lógica de routing por hora
2. Revisar `scripts/com.fortresscore.pipeline.plist` — schedule y paths
3. Confirmar que el fix de `_make_client_factory` ya está en main (commit e439a3a)

## Instalación (cuando Boris autorice)
```bash
# Copiar plist a ~/Library/LaunchAgents/
cp scripts/com.fortresscore.pipeline.plist ~/Library/LaunchAgents/
# Cargar
launchctl load ~/Library/LaunchAgents/com.fortresscore.pipeline.plist
# Verificar
launchctl list | grep fortress
```

## Modo observación (1-2 semanas post-instalación)
- NO tocar definición de señal ni reoptimizar
- Solo observar que corra bien sola
- Revisar logs diarios en `scripts/pipeline_diario.log`
